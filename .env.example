# Example .env file for local development with local-server.ts
# Copy this file to .env and fill in your actual values.

# Required: Comma-separated list of your Gemini API keys, or a JSON array string.
# Example 1: Comma-separated
# API_KEYS=your_api_key_1,your_api_key_2
# Example 2: JSON array string (useful if keys contain commas or special chars)
# API_KEYS=["your_api_key_1","your_api_key_2"]
API_KEYS=your_gemini_api_key_here

# Optional: Override the default Gemini API base URL
# GEMINI_API_BASE_URL=https://generativelanguage.googleapis.com/v1beta2

# Optional: Set an access token to protect your local server endpoint.
# If set, requests to /api/* must include an "X-Access-Token" header with this value.
# ACCESS_TOKEN=your_secret_access_token
